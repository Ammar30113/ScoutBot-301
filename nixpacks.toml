providers = ["python"]

[variables]
PYTHON_VERSION = "3.11"

[phases.install]
cmds = [
  "python -m pip install --upgrade pip",
  "python -m pip install --no-cache-dir -r requirements.txt"
]

[start]
cmd = "python main.py"
